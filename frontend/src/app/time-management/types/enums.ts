export enum PunchPolicy {
  FIRST_LAST = "FIRST_LAST",
  MULTIPLE = "MULTIPLE",
}

export enum ShiftAssignmentStatus {
  PENDING = "PENDING",
  APPROVED = "APPROVED",
  CANCELLED = "CANCELLED",
  EXPIRED = "EXPIRED",
  REJECTED = "REJECTED",
  SUBMITTED = "SUBMITTED",
}
